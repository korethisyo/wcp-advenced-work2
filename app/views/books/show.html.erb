<div class='container'>
  <div class='row'>
    <div class='col-md-3'>
      <h2>User info</h2>
      <table class='table'>
        <tr><%= attachment_image_tag @book.user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg" %></tr>
        <tr>
	        <th>name</th>
	        <th><%= @book.user.name %></th>
        </tr>
        <tr>
	        <th>introduction</th>
	        <th><%= @book.user.introduction %></th>
        </tr>
      </table>

      <div class='row'>
        <%= link_to '',edit_user_path(@book.user.id),class: "btn btn-outline-secondary btn-block fas fa-user-cog" %>
      </div>
      <h2 class="mt-3">New book</h2>
      <%= render 'form', book: @new_book %>
    </div>
    <div class='col-md-8 offset-md-1'>
  		<h2>Book detail</h2>
  		<table class='table'>
  		  <tr id="book_<%= @book.id %>">
  		    <%= render 'books/show', book: @book %>
        </tr>
      </table>

      <div class="comments">
        <% @book.book_comments.each do |book_comment| %>
          <%= attachment_image_tag(book_comment.user, :profile_image, :fill, 40, 40, fallback: "no-image-icon.ipg") %>
          <%= link_to book_comment.user.name, user_path(book_comment.user.id) %>
          <%= book_comment.comment %>
          <% if book_comment.user == current_user %>
            <div class="comment-delete">
              <%= link_to 'Destroy', book_book_comment_path(book_comment.book.id, book_comment.id), method: :delete, class: "btn btn-sm btn-danger" %>
            </div>
          <% end %>
        <% end %>
      </div>

      <div class="new-comment">
        <%= form_with(model:[@book, @book_comment], local:true) do |f| %>
          <%= f.text_area :comment, rows: '5', class: 'form-control book_comment my-2' %>
          <%= f.submit '送信' %>
        <% end %>
      </div>
    </div>
  </div>
</div>